{
  "name": "Python Flask App",
  "forwardPorts": [5000],
  "postCreateCommand": "pip install -r requirements.txt",
  "portsAttributes": {
    "5000": {
      "label": "Flask App",
      "onAutoForward": "openBrowser"
    }
  }
}